#include<stdio.h>
int main()
{
	int i = 1, n;
	scanf("%d",&n);
	while (i<=n)
	{
		printf("%d\n",i);
		i++;
//		sum = sum +i; // for calculating sum of first n numbers.
//		sum = sum+n; // for sum of n number entered by users
	}
	
	
}
